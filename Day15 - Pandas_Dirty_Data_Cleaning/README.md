# 1. Read the CSV

# 2. Print info — how many nulls per column (df.isnull().sum())

# 3. Remove duplicate rows

# 4. Fill missing salaries with the column average

# 5. Drop rows where name is missing

# 6. Fix department column — title case (Engineering not engineering)

# 7. Fix email column — lowercase

# 8. Fix age column — convert to numeric, invalid values become NaN

# (pd.to_numeric with errors="coerce")

# 9. Remove rows where age > 100 or age is NaN

# 10. Save cleaned data to clean_data.csv

# 11. Print how many rows before and after cleaning
